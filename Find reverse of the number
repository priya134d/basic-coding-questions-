#include <bits/stdc++.h>
using namespace std;

int main()
{
 int num, reverse=0;
 cin>>num;
 cout<<"reverse of the number"<<endl;
while(num!=0){
 int rem = num%10;
 reverse = (reverse*10) + rem;
 num = num/10;     
 }
 cout<<reverse;

    return 0;
}
